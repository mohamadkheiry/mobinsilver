# مشارکت در توسعه

از مشارکت در مبین سیلور استقبال می‌شود. پیش از تغییر، [راهنمای توسعه](development.md)، [معماری](docs/architecture.md) و [راهبرد تست](docs/testing.md) را بخوانید.

فرآیند کوتاه:

1. یک Issue یا شرح روشن برای تغییر داشته باشید.
2. branch با پیشوند `feature/`، `fix/`، `docs/` یا `chore/` بسازید.
3. تغییر را کوچک، متمرکز و همراه تست نگه دارید.
4. build کامل backend و frontend را اجرا کنید.
5. مستندات و نمودارهای متاثر را به‌روزرسانی کنید.
6. Pull Request با شواهد تست و اسکرین‌شات UI ثبت کنید.

هیچ secret، token، دیتابیس، اطلاعات مشتری یا خروجی build را commit نکنید. Definition of Done و قالب commit در [development.md](development.md) مستند شده است.

